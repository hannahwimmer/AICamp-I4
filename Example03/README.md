# AICamp Coding Beispiele Setup

1. Eine virtuelle `uv`-Umgebung für das Projekt herstellen (aus dem zur Verfügung
   gestellten `.toml`-file):
   `uv sync`
2. Um Dagster zu starten:
   `dagster dev -f pipeline.py`